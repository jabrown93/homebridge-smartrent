---
name: Release
# Keep this file named release.yml and keep the push trigger: npm trusted
# publishing matches on this repo + this entry-point filename.
on:
  push:
    branches:
      - main
      - beta
      - alpha
      - next
  # Weekly roll-up of the fix(deps) commits that .releaserc.js suppresses on
  # push, promoted into a single patch release. GitHub only fires schedule
  # from the default branch, so this sweeps main only; use the manual trigger
  # below to sweep beta.
  schedule:
    - cron: '0 9 * * 1' # Mondays 09:00 UTC — weekly dependency roll-up
  # Also sets RELEASE_DEPS, so a manual run is an on-demand dependency sweep
  # (and the only way to promote beta's suppressed fix(deps) commits).
  workflow_dispatch: {}
concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false
jobs:
  release:
    uses: jabrown93/ci/.github/workflows/npm-release.yml@28ec69f555a61dd8770c3814b157ef2c50f1f8d5 # workflows-v2.0.0
    permissions:
      contents: write
      issues: write
      pull-requests: write
      id-token: write
    secrets:
      APP_ID: ${{ secrets.APP_ID }}
      APP_PRIVATE_KEY: ${{ secrets.APP_PRIVATE_KEY }}
