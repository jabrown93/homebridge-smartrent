---
name: 'CodeQL Advanced'
on:
  push:
    branches: [main, beta]
  pull_request:
    branches: [main, beta]
  schedule:
    - cron: '25 22 * * 5'
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true
jobs:
  analyze:
    # codeql is now a composite action, so the caller owns runs-on and MUST
    # grant the permissions CodeQL needs. Language defaults to
    # javascript-typescript / build-mode none, which is correct for this repo.
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      packages: read
      actions: read
      contents: read
    steps:
      - uses: jabrown93/ci/actions/codeql@520b66a5b7808a3ca277b935afb35d5d7b311d52 # codeql-v1.0.4
